-r requirements.txt
pytest==8.3.4
httpx==0.27.2
aiosqlite==0.20.0
pytest-cov==7.1.0

# Phase T6: Performance, Load, and Reliability Testing — never installed
# into a production image (see Dockerfile), same reasoning as every
# other entry in this file.
locust==2.46.1
psutil==7.2.2

# Volume 4 Phase 2: Continuous Integration — static analysis run in CI
# (and locally) before tests, so obviously-broken code never even
# reaches the test suite.
ruff==0.15.22
mypy==2.3.0

# Volume 4 Phase 5: Security Hardening — dependency vulnerability
# scanning (see .github/workflows/ci.yml).
pip-audit==2.10.1
